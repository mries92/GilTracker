mod game_scanner;
use game_scanner::ScanResult;

/// Handles reading and writing scan results to disk
pub struct FileManager {
}

impl FileManager {
    /// Read the entire cached data file into memory
    pub fn read_data_from_disk() {

    }

    /// Append a data entry to the file on disk
    pub fn write_data_to_disk(data: ScanResult) {

    }
}